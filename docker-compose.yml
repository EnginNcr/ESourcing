version: '3.4'

services:
  sourcingdb:
    image: mongo

  esourcingproducts:
    image: ${DOCKER_REGISTRY-}esourcingproducts
    build:
      context: .
      dockerfile: ESourcingProducts/Dockerfile
